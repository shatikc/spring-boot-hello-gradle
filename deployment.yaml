apiVersion: apps/v1
kind: Deployment
metadata:
  name: $JOB_NAME
  namespace: $NAMESPACE
spec:
  selector:
    matchLabels:
      app: $JOB_NAME
  template:
    metadata:
      labels:
        app: $JOB_NAME
    spec:
      containers:
      - name: $JOB_NAME
        image: $DOCKER_HUB_ID/$JOB_NAME:$BUILD_NUMBER
        imagePullPolicy: Always
        resources:
          limits:
            memory: "0"
            cpu: "0"
        ports:
        - containerPort: 8080
          name: http-port
          protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: $JOB_NAME
  namespace: $NAMESPACE
spec:
  selector:
    app: $JOB_NAME
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 32112
  type: NodePort
